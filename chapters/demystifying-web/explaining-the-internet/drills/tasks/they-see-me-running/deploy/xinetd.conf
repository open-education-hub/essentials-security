service name-database
{
    disable		= no
    socket_type		= stream
    protocol		= tcp
    wait		= no
    user		= ctf
    bind		= 0.0.0.0
    server		= /tmp/.server
    port		= 31337
    type		= UNLISTED
}
